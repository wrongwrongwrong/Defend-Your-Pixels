// Aligns naming with model_backend/game (Python) + game_mech_pitch.txt

export const GRID_SIZE = 12;
export const CELL_SIZE = 56;

/** Matches model_backend.entities.UnitKind values */
export const UNIT_KIND = {
  ATTACKER: "attacker",
  DEFENDER: "defender",
};

/** Matches model_backend.types.PlayerId */
export const PLAYER_ID = {
  P1: 1,
  P2: 2,
};

export const COMMAND_TOWER_MAX_HP = 20;
