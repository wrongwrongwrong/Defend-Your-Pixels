# C++ Reference

`main.cpp` is intentionally preserved in the repository root.

Reason
- Part of the tracking / AR prototype was first explored in C++.
- The Python implementation references ideas and flow from that earlier C++ code.
- Keeping the file makes the conversion history explicit and gives a clear interview narrative: the implementation started from a C++ reference and was then adapted into Python for faster iteration.

Interview-safe summary
- Original prototype logic was explored in C++.
- The current working tracker stack is Python-based.
- `main.cpp` is preserved as a reference artifact, not as the main runtime entry point.
