# State Output

Purpose
- build normalized tracker snapshots from camera observations
- prepare tracker-side data before it is adapted by the bridge layer

Current files
- `tracker_snapshot.py`: tracker snapshot builder and annotated preview helper
