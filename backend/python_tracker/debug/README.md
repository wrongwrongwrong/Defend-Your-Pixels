# Debug

Purpose
- hold lightweight debug-only preview scripts
- avoid mixing quick camera previews with the live tracker runtime

Current files
- `camera_preview.py`: raw camera preview without marker detection
